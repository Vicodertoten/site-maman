# Example env file — copy to .env and fill with real values (do NOT commit .env)
SANITY_PROJECT_ID=your_project_id
SANITY_DATASET=production
SANITY_API_VERSION=v2024-01-01
SANITY_AUTH_TOKEN=your_sanity_token

# Site
PUBLIC_SITE_URL=https://www.example.com

# Stripe
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Supabase (if used)
SUPABASE_URL=https://your-supabase-url
SUPABASE_SERVICE_ROLE_KEY=replace-me
SUPABASE_STORAGE_BUCKET=ebooks

# Emails (Resend)
RESEND_API_KEY=re_...
ACCESS_EMAIL_FROM="Gastronomade <bonjour@gastronomade.fr>"
ACCESS_EMAIL_REPLY_TO=bonjour@gastronomade.fr

# Notes:
# - If you have leaked a key, revoke it immediately and generate a new one.
# - To remove secrets from git history use `git filter-repo` or the BFG tool; contact an admin if unsure.
